import { Typography } from '@/components/ui/typography';

export function Footer() {
  return (
    <footer className="flex justify-center px-8 py-4 border-t-muted border-t bg-background border-solid">
      <Typography muted>Footer</Typography>
    </footer>
  );
}
